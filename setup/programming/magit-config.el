;;; package ---- "magit-config.el" Created by Roger Molas on 11/14/17.
;;; Commentary:
;;;    Customization and configuration for magit
;;; Code:

(add-to-list 'load-path "~/.emacs.d/elpa/magit-20171112.34")
(global-set-key (kbd "s-0") 'magit-status)
(provide 'magit-config) ;;; magit-config.el ends here
