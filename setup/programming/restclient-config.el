;;; package ---- "resclient-config.el" Created by Roger Molas on 07/30/18.
;;; Commentary:
;;;    Customization and configuration for resclient
;;; Code:

(add-to-list 'load-path "~/.emacs.d/plugin-lisp/restclient")
(require 'restclient)
(provide 'restclient-config) ;;; resclient-config.el ends here
