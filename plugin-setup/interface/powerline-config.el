;;; package ---- "powerline-config.el" Created by Roger Molas on 07/11/15.
;;; Commentary:
;;;    Customization and configuration for powerline
;;; Code:

(add-to-list 'load-path "~/.emacs.d/plugin-lisp/powerline")

(require 'powerline)
(require 'cl)
(setq powerline-arrow-shape 'arrow14)   ;; the default

(provide 'powerline-config)
;;; powerline-config.el ends here
